# Python Suggestion Service

This small Flask service receives recent expense data and returns budget suggestions.

## Run locally

```bash
pip install -r requirements.txt
python app.py
```

The backend expects it at `http://localhost:5001` by default.
